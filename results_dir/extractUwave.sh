#!bin/bash

# note that this assumes that the uwave stuff is in a particular place,
# and it probably isn't where you have it

unrarFiles.sh ../uWaveGestureLibrary/origData ../uWaveGestureLibrary/extracted
